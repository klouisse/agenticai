#__init__.py was used to make the src directory a package. It can be empty or contain initialization code for the package. In this case, it is empty.
# This makes it importable
# Inside langgraphagenticai, we can have an __init__.py file that imports the necessary modules
#  and classes to make them accessible when we import the package. 
# For example, we can import the main agent class and any utility functions or classes that are needed for the agent to function properly.
#  This way, when we import langgraphagenticai, we can access all the necessary components of the package without having to import each module separately.